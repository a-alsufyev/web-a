version: "3.9"

services:
  app:
    build: .
    container_name: ai-studio-app
    ports:
      - "3010:3000"
    environment:
      NODE_ENV: production
      GEMINI_API_KEY: ${GEMINI_API_KEY}
    restart: always